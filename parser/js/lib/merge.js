module.exports = merge;

/**
 * Módulo para mesclar los geodatos con los metadatos.
 * @param {Array} dataset Es una lista javascript con dos itenes. 
 */
function merge(dataset) {
  process.stdout.write(dataset)
}
